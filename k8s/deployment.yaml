apiVersion: apps/v1
kind: Deployment
metadata:
  name: agromind-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: agromind-backend
          ports:
            - containerPort: 5000
